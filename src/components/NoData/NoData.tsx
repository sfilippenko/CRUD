import * as React from 'react';
import Typography from '@material-ui/core/Typography';

const NoData: React.FC = () => {
  return <Typography variant="h6">Данных нет</Typography>;
};

export default NoData;
