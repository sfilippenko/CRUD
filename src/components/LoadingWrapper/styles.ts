import createStyles from '@material-ui/core/styles/createStyles';

export default createStyles({
  wrapper: {
    position: 'relative',
  },
  progress: {
    position: 'absolute',
    top: 0,
    left: 0,
    width: '100%',
  },
});
